<div class="navbar">
<ul>
  <h1>Moments Captured</h1>
  <% if current_user %>
        <div>
          <%= link_to "My Profile", profile_path(current_user.profile),class:"navlink"%>
        </div>
        <% else%>
          <div> 
            <%= link_to "Sign Up", new_user_registration_path, class:"navlink"%>
          </div>
          <div>
            <%= link_to "Login", new_user_session_url,class:"navlink" %>
        <% end%>
        <% if current_user %>
        <div>
          <%= link_to 'Jobs', photography_jobs_path, class:"navlink"%>
        </div>
        <div>
          <%= link_to 'Create Job', new_photography_job_path, class:"navlink"%>
        </div >
  <% end %>
  <% if current_user%>
          <div >
            <%= button_to 'Logout', destroy_user_session_url, method: :delete%>
          </div>
          <% else %>
          
          </div>
  <% end%>
</ul>
</div>
